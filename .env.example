# (C) Claude-generated — template for local secrets.
# Copy this file to ".env" and paste your real key. NEVER commit ".env".
#   cp .env.example .env
#
# Not needed for the scraper (slice #1) — only for the translator (slice #3).
DEEPL_API_KEY=your_deepl_api_key_here
