# Secrets — NEVER commit
.env

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# Scraped data / outputs (but keep the matched seed so deploys can rebuild the DB)
data/*
!data/listings_matched.json

# OS
.DS_Store
